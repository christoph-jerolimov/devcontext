{
  "name": "@devcontext/shared",
  "version": "0.1.0",
  "private": true,
  "description": "The wire contract between the devcontext server and its viewers: the payload types every /api endpoint answers with, declared once.",
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=22.5"
  },
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "7.0.2"
  }
}
