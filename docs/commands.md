<!--
  Generated by "npm run docs" from the command definitions.
  Do not edit by hand: cli/src/docs/reference.ts renders it, and a test fails
  when this file and the program disagree.

  For what the commands are *for*, start at README.md in this directory. This
  page is the exhaustive list of every command, argument and option.
-->

# Command reference

Every command, argument and option, taken straight from the program. For what
each command is _for_, and for worked examples, see the pages it links to:
[Sync](sync.md), [Search](search.md), [Insights](insights.md),
[Digest](digest.md), [Cross links](links.md), [Audit](audit.md),
[Web viewer](web.md) and [MCP server](mcp.md).

```
devcontext [options] <command>
```

## Global options

| Option | Description |
| --- | --- |
| `-V, --version` | output the version number |
| `-c, --config <path>` | path to devcontext.yaml |
| `--db <path>` | override the database path from the configuration |
| `-v, --verbose` | log every request |
| `-q, --quiet` | only log errors |

## Commands

- [`init`](#devcontext-init) — write a devcontext.yaml, detecting the GitHub repository of this directory
- [`sync`](#devcontext-sync) — fetch GitHub and Jira data into the SQLite database and the configured outputs
- [`status`](#devcontext-status) — show the configuration, what is in the database and the last sync runs
- [`export`](#devcontext-export) — write the yaml / markdown / json outputs again from the database
- [`search`, `find`, `q`](#devcontext-search) — full text search across issues, pull requests, work items and their comments
- [`github`, `gh`](#devcontext-github) — read GitHub data from the local database
  - [`repos`, `repo`, `repositories`, `repository`](#devcontext-github-repos) — list the repositories that have been synced
  - [`issues`, `issue`](#devcontext-github-issues) — list issues, or show one issue with all comments and events
  - [`prs`, `pr`, `pullrequest`, `pullrequests`, `pull-request`, `pull-requests`](#devcontext-github-prs) — list pull requests, or show one with reviews, commits and files
  - [`workflows`, `workflow`, `actions`, `action`](#devcontext-github-workflows) — list the GitHub Actions workflows of the synced repositories
  - [`runs`, `run`](#devcontext-github-runs) — list workflow runs, or show one run with its jobs and steps
  - [`jobs`, `job`](#devcontext-github-jobs) — list workflow jobs
  - [`steps`, `step`](#devcontext-github-steps) — list workflow steps
  - [`logs`, `log`](#devcontext-github-logs) — print the stored log of a workflow job
- [`jira`](#devcontext-jira) — read Jira data from the local database
  - [`workitems`, `workitem`, `items`, `item`](#devcontext-jira-workitems) — list work items, or show one with comments and full history
  - [`stories`, `story`](#devcontext-jira-stories) — list stories (work items of type story)
  - [`epics`, `epic`](#devcontext-jira-epics) — list epics (work items of type epic)
  - [`features`, `feature`](#devcontext-jira-features) — list features (work items of type feature)
  - [`outcomes`, `outcome`](#devcontext-jira-outcomes) — list outcomes (work items of type outcome)
  - [`bugs`, `bug`](#devcontext-jira-bugs) — list bugs (work items of type bug)
  - [`tasks`, `task`](#devcontext-jira-tasks) — list tasks (work items of type task)
  - [`tree`](#devcontext-jira-tree) — show the parents and children of a work item as a tree
  - [`search`](#devcontext-jira-search) — search work items by key, summary, description and comments
  - [`sprints`, `sprint`](#devcontext-jira-sprints) — list sprints, or show one sprint with its work items
  - [`projects`, `project`](#devcontext-jira-projects) — list the Jira projects that have been synced
  - [`fields`, `field`](#devcontext-jira-fields) — list Jira fields and the friendly names configured for them
- [`tickets`, `ticket`](#devcontext-tickets) — GitHub issues and Jira work items as one list
- [`activity`, `feed`, `changes`](#devcontext-activity) — status changes, comments and reviews across GitHub and Jira, newest first
- [`people`, `person`](#devcontext-people) — the configured people and bots, with their GitHub and Jira identities
- [`teams`, `team`](#devcontext-teams) — the configured teams and their members
- [`serve`, `web`](#devcontext-serve) — serve the React viewer and a JSON API for the local database
- [`insights`, `report`, `stats`](#devcontext-insights) — cycle time, review latency, work in progress, stale items, flaky steps, sprints
- [`digest`, `standup`, `summary`](#devcontext-digest) — what happened in a window: shipped, started, reviewed, and still stuck
- [`links`, `link`](#devcontext-links) — cross references between GitHub issues/pull requests and Jira work items
- [`contributors`, `who`, `contributor`](#devcontext-contributors) — who worked on an issue, pull request or work item, and what they did
- [`history`](#devcontext-history) — how many issues, pull requests or work items were open over time
- [`audit`](#devcontext-audit) — what is stored locally, who is in it, and what a sync would fetch
- [`mcp`](#devcontext-mcp) — serve the local database over the Model Context Protocol (stdio)
- [`agent`](#devcontext-agent) — start the @devcontext/eve agent in dev mode (experimental)

## `devcontext init`

Write a devcontext.yaml, detecting the GitHub repository of this directory

```
devcontext init [options]
```

| Option | Default | Description |
| --- | --- | --- |
| `-f, --force` | — | overwrite an existing configuration file |
| `--path <file>` | `devcontext.yaml` | where to write the configuration |
| `--example` | — | write the fully commented example instead of a detected configuration |
| `--detect` | — | detect only: print what was found and write nothing |
| `--repo <repo>` | — | use this repository instead of the detected one, repeatable |
| `--all-remotes` | — | include every git remote, not just the first one (origin) |
| `--project <key>` | — | project key to use in the generated configuration |
| `--since <when>` | `12mo` | how far back the initial sync should reach |

## `devcontext sync`

Fetch GitHub and Jira data into the SQLite database and the configured outputs

```
devcontext sync [options]
```

| Option | Values | Default | Description |
| --- | --- | --- | --- |
| `-p, --project <key>` | — | — | only sync this project, repeatable |
| `-s, --source <source>` | — | — | github or jira, repeatable |
| `-t, --target <name>` | — | — | only sync this repository or Jira project, repeatable |
| `--only <ref>` | — | — | sync just this item first: owner/name#42, 42 or PLAT-42, repeatable |
| `--only-targeted` | — | — | stop after the --only items instead of continuing with the rest |
| `--full` | — | — | ignore stored cursors and download everything again |
| `--resume` | — | — | skip what the last failed or interrupted run already finished |
| `--dry-run` | — | — | fetch from the APIs without writing anything |
| `--no-progress` | — | — | do not render the progress indicator |
| `--no-outputs` | — | — | skip the yaml / markdown / json outputs |
| `--delay <ms>` | — | — | minimum delay between two API calls, overrides the configuration |
| `-o, --output <format>` | `default`, `json`, `markdown`, `plain` | `default` | output format |
| `--list` | — | — | print bare identifiers only, one per line (for shell scripts) |

## `devcontext status`

Show the configuration, what is in the database and the last sync runs

```
devcontext status [options]
```

| Option | Values | Default | Description |
| --- | --- | --- | --- |
| `-n, --limit <count>` | — | `10` | number of sync runs to show |
| `-o, --output <format>` | `default`, `json`, `markdown`, `plain` | `default` | output format |
| `--list` | — | — | print bare identifiers only, one per line (for shell scripts) |

## `devcontext export`

Write the yaml / markdown / json outputs again from the database

```
devcontext export [options]
```

| Option | Values | Default | Description |
| --- | --- | --- | --- |
| `-r, --repo <repo>` | — | — | only this repository, repeatable |
| `-p, --project <key>` | — | — | only this Jira project key, repeatable |
| `--no-workflow-runs` | — | — | skip the workflow run documents |
| `-o, --output <format>` | `default`, `json`, `markdown`, `plain` | `default` | output format |
| `--list` | — | — | print bare identifiers only, one per line (for shell scripts) |

## `devcontext search`

Aliases: `find`, `q`

Full text search across issues, pull requests, work items and their comments

```
devcontext search [options] [query...]
```

| Argument | Required | Description |
| -------- | -------- | ----------- |
| `query...` | no | words to look for; quote a phrase to match it exactly |

| Option | Values | Default | Description |
| --- | --- | --- | --- |
| `-k, --kind <kind>` | — | — | restrict to issue, pull-request, workitem, repeatable |
| `-r, --repo <repo>` | — | — | GitHub repository, repeatable |
| `-p, --project <key>` | — | — | Jira project key, repeatable |
| `--exact` | — | — | do not treat the last word as a prefix |
| `-n, --limit <count>` | — | `25` | maximum number of hits |
| `--offset <count>` | — | — | skip this many hits |
| `--rebuild` | — | — | rebuild the index and exit |
| `-o, --output <format>` | `default`, `json`, `markdown`, `plain` | `default` | output format |
| `--list` | — | — | print bare identifiers only, one per line (for shell scripts) |

## `devcontext github`

Alias: `gh`

Read GitHub data from the local database

```
devcontext github <command>
```

### `devcontext github repos`

Aliases: `repo`, `repositories`, `repository`

List the repositories that have been synced

```
devcontext github repos [options]
```

| Option | Values | Default | Description |
| --- | --- | --- | --- |
| `-o, --output <format>` | `default`, `json`, `markdown`, `plain` | `default` | output format |
| `--list` | — | — | print bare identifiers only, one per line (for shell scripts) |
| `-n, --limit <count>` | — | `50` | maximum number of rows (0 for no limit) |
| `--offset <count>` | — | — | skip this many rows |
| `--search <text>` | — | — | match text in the title / summary / body |

### `devcontext github issues`

Alias: `issue`

List issues, or show one issue with all comments and events

```
devcontext github issues [options] [number]
```

| Argument | Required | Description |
| -------- | -------- | ----------- |
| `number` | no | issue number to show in detail |

| Option | Values | Default | Description |
| --- | --- | --- | --- |
| `-r, --repo <repo>` | — | — | repository (owner/name), repeatable |
| `-s, --state <state>` | — | `all` | open, closed or all |
| `-l, --label <label>` | — | — | label filter, repeatable |
| `-a, --author <login>` | — | — | issue author |
| `--assignee <login>` | — | — | assigned to this user |
| `--milestone <title>` | — | — | milestone title |
| `--sort <field>` | — | `updated` | updated, created or number |
| `--order <direction>` | — | `desc` | asc or desc |
| `--created-since <when>` | — | — | created at or after (30d, 6w, 2024-01-31) |
| `--created-before <when>` | — | — | created before (30d, 6w, 2024-01-31) |
| `--updated-since <when>` | — | — | updated at or after (30d, 6w, 2024-01-31) |
| `--updated-before <when>` | — | — | updated before, i.e. untouched since then |
| `--stale <duration>` | — | — | only items not updated for this long (e.g. 90d) |
| `--person <id>` | — | — | only items involving this configured person, repeatable |
| `--team <id>` | — | — | only items involving a member of this team, repeatable |
| `--me` | — | — | shorthand for --person <the id me: names in devcontext.yaml> |
| `--no-bots` | — | — | hide items written by a bot |
| `--bots-only` | — | — | only items written by a bot |
| `-o, --output <format>` | `default`, `json`, `markdown`, `plain` | `default` | output format |
| `--list` | — | — | print bare identifiers only, one per line (for shell scripts) |
| `-n, --limit <count>` | — | `50` | maximum number of rows (0 for no limit) |
| `--offset <count>` | — | — | skip this many rows |
| `--search <text>` | — | — | match text in the title / summary / body |

### `devcontext github prs`

Aliases: `pr`, `pullrequest`, `pullrequests`, `pull-request`, `pull-requests`

List pull requests, or show one with reviews, commits and files

```
devcontext github prs [options] [number]
```

| Argument | Required | Description |
| -------- | -------- | ----------- |
| `number` | no | pull request number to show in detail |

| Option | Values | Default | Description |
| --- | --- | --- | --- |
| `-r, --repo <repo>` | — | — | repository (owner/name), repeatable |
| `-s, --state <state>` | — | `all` | open, closed or all |
| `-l, --label <label>` | — | — | label filter, repeatable |
| `-a, --author <login>` | — | — | pull request author |
| `--assignee <login>` | — | — | assigned to this user |
| `--reviewer <login>` | — | — | reviewed by this user |
| `--base <ref>` | — | — | target branch |
| `--draft` | — | — | only drafts |
| `--no-draft` | — | — | exclude drafts |
| `--merged` | — | — | only merged pull requests |
| `--no-merged` | — | — | only unmerged pull requests |
| `--sort <field>` | — | `updated` | updated, created or number |
| `--order <direction>` | — | `desc` | asc or desc |
| `--created-since <when>` | — | — | created at or after (30d, 6w, 2024-01-31) |
| `--created-before <when>` | — | — | created before (30d, 6w, 2024-01-31) |
| `--updated-since <when>` | — | — | updated at or after (30d, 6w, 2024-01-31) |
| `--updated-before <when>` | — | — | updated before, i.e. untouched since then |
| `--stale <duration>` | — | — | only items not updated for this long (e.g. 90d) |
| `--person <id>` | — | — | only items involving this configured person, repeatable |
| `--team <id>` | — | — | only items involving a member of this team, repeatable |
| `--me` | — | — | shorthand for --person <the id me: names in devcontext.yaml> |
| `--no-bots` | — | — | hide items written by a bot |
| `--bots-only` | — | — | only items written by a bot |
| `-o, --output <format>` | `default`, `json`, `markdown`, `plain` | `default` | output format |
| `--list` | — | — | print bare identifiers only, one per line (for shell scripts) |
| `-n, --limit <count>` | — | `50` | maximum number of rows (0 for no limit) |
| `--offset <count>` | — | — | skip this many rows |
| `--search <text>` | — | — | match text in the title / summary / body |

### `devcontext github workflows`

Aliases: `workflow`, `actions`, `action`

List the GitHub Actions workflows of the synced repositories

```
devcontext github workflows [options]
```

| Option | Values | Default | Description |
| --- | --- | --- | --- |
| `-r, --repo <repo>` | — | — | repository (owner/name), repeatable |
| `-o, --output <format>` | `default`, `json`, `markdown`, `plain` | `default` | output format |
| `--list` | — | — | print bare identifiers only, one per line (for shell scripts) |
| `-n, --limit <count>` | — | `50` | maximum number of rows (0 for no limit) |
| `--offset <count>` | — | — | skip this many rows |
| `--search <text>` | — | — | match text in the title / summary / body |

### `devcontext github runs`

Alias: `run`

List workflow runs, or show one run with its jobs and steps

```
devcontext github runs [options] [id]
```

| Argument | Required | Description |
| -------- | -------- | ----------- |
| `id` | no | workflow run id to show in detail |

| Option | Values | Default | Description |
| --- | --- | --- | --- |
| `-r, --repo <repo>` | — | — | repository (owner/name), repeatable |
| `-w, --workflow <name>` | — | — | workflow name or file path |
| `--status <status>` | — | — | queued, in_progress or completed |
| `--conclusion <conclusion>` | — | — | success, failure, cancelled, skipped, ... |
| `--branch <ref>` | — | — | head branch |
| `--event <event>` | — | — | trigger event (push, pull_request, schedule, ...) |
| `--actor <login>` | — | — | user that triggered the run |
| `--created-since <when>` | — | — | created at or after (30d, 6w, 2024-01-31) |
| `--created-before <when>` | — | — | created before (30d, 6w, 2024-01-31) |
| `--updated-since <when>` | — | — | updated at or after (30d, 6w, 2024-01-31) |
| `--updated-before <when>` | — | — | updated before, i.e. untouched since then |
| `--stale <duration>` | — | — | only items not updated for this long (e.g. 90d) |
| `-o, --output <format>` | `default`, `json`, `markdown`, `plain` | `default` | output format |
| `--list` | — | — | print bare identifiers only, one per line (for shell scripts) |
| `-n, --limit <count>` | — | `50` | maximum number of rows (0 for no limit) |
| `--offset <count>` | — | — | skip this many rows |
| `--search <text>` | — | — | match text in the title / summary / body |

### `devcontext github jobs`

Alias: `job`

List workflow jobs

```
devcontext github jobs [options] [runId]
```

| Argument | Required | Description |
| -------- | -------- | ----------- |
| `runId` | no | only jobs of this workflow run |

| Option | Values | Default | Description |
| --- | --- | --- | --- |
| `-r, --repo <repo>` | — | — | repository (owner/name), repeatable |
| `--conclusion <conclusion>` | — | — | success, failure, cancelled, skipped, ... |
| `-o, --output <format>` | `default`, `json`, `markdown`, `plain` | `default` | output format |
| `--list` | — | — | print bare identifiers only, one per line (for shell scripts) |
| `-n, --limit <count>` | — | `50` | maximum number of rows (0 for no limit) |
| `--offset <count>` | — | — | skip this many rows |
| `--search <text>` | — | — | match text in the title / summary / body |

### `devcontext github steps`

Alias: `step`

List workflow steps

```
devcontext github steps [options] [jobId]
```

| Argument | Required | Description |
| -------- | -------- | ----------- |
| `jobId` | no | only steps of this job |

| Option | Values | Default | Description |
| --- | --- | --- | --- |
| `--run <runId>` | — | — | only steps of this workflow run |
| `--conclusion <conclusion>` | — | — | success, failure, cancelled, skipped, ... |
| `-o, --output <format>` | `default`, `json`, `markdown`, `plain` | `default` | output format |
| `--list` | — | — | print bare identifiers only, one per line (for shell scripts) |
| `-n, --limit <count>` | — | `50` | maximum number of rows (0 for no limit) |
| `--offset <count>` | — | — | skip this many rows |
| `--search <text>` | — | — | match text in the title / summary / body |

### `devcontext github logs`

Alias: `log`

Print the stored log of a workflow job

```
devcontext github logs [options] <jobId>
```

| Argument | Required | Description |
| -------- | -------- | ----------- |
| `jobId` | yes | workflow job id |

| Option | Values | Default | Description |
| --- | --- | --- | --- |
| `-o, --output <format>` | `default`, `json`, `markdown`, `plain` | `default` | output format |
| `--list` | — | — | print bare identifiers only, one per line (for shell scripts) |

## `devcontext jira`

Read Jira data from the local database

```
devcontext jira <command>
```

### `devcontext jira workitems`

Aliases: `workitem`, `items`, `item`

List work items, or show one with comments and full history

```
devcontext jira workitems [options] [key]
```

| Argument | Required | Description |
| -------- | -------- | ----------- |
| `key` | no | work item key (e.g. PROJ-123) to show in detail |

| Option | Values | Default | Description |
| --- | --- | --- | --- |
| `-p, --project <key>` | — | — | Jira project key, repeatable |
| `--site <name>` | — | — | Jira site name from the configuration |
| `--status <status>` | — | — | status name, repeatable |
| `--category <category>` | — | — | "To Do", "In Progress" or "Done" |
| `--assignee <name>` | — | — | assignee display name (substring match) |
| `--reporter <name>` | — | — | reporter display name (substring match) |
| `-l, --label <label>` | — | — | label filter, repeatable |
| `--component <name>` | — | — | component filter, repeatable |
| `--sprint <name>` | — | — | sprint name (substring match) |
| `--epic <key>` | — | — | items belonging to this epic |
| `--parent <key>` | — | — | items with this parent |
| `--open` | — | — | only unresolved work items |
| `--resolved` | — | — | only resolved work items |
| `--sort <field>` | — | `updated` | updated, created, key or status |
| `--order <direction>` | — | `desc` | asc or desc |
| `-t, --type <type>` | — | — | work item type, repeatable |
| `--created-since <when>` | — | — | created at or after (30d, 6w, 2024-01-31) |
| `--created-before <when>` | — | — | created before (30d, 6w, 2024-01-31) |
| `--updated-since <when>` | — | — | updated at or after (30d, 6w, 2024-01-31) |
| `--updated-before <when>` | — | — | updated before, i.e. untouched since then |
| `--stale <duration>` | — | — | only items not updated for this long (e.g. 90d) |
| `-o, --output <format>` | `default`, `json`, `markdown`, `plain` | `default` | output format |
| `--list` | — | — | print bare identifiers only, one per line (for shell scripts) |
| `-n, --limit <count>` | — | `50` | maximum number of rows (0 for no limit) |
| `--offset <count>` | — | — | skip this many rows |
| `--search <text>` | — | — | match text in the title / summary / body |

### `devcontext jira stories`

Alias: `story`

List stories (work items of type story)

```
devcontext jira stories [options] [key]
```

| Argument | Required | Description |
| -------- | -------- | ----------- |
| `key` | no | work item key (e.g. PROJ-123) to show in detail |

| Option | Values | Default | Description |
| --- | --- | --- | --- |
| `-p, --project <key>` | — | — | Jira project key, repeatable |
| `--site <name>` | — | — | Jira site name from the configuration |
| `--status <status>` | — | — | status name, repeatable |
| `--category <category>` | — | — | "To Do", "In Progress" or "Done" |
| `--assignee <name>` | — | — | assignee display name (substring match) |
| `--reporter <name>` | — | — | reporter display name (substring match) |
| `-l, --label <label>` | — | — | label filter, repeatable |
| `--component <name>` | — | — | component filter, repeatable |
| `--sprint <name>` | — | — | sprint name (substring match) |
| `--epic <key>` | — | — | items belonging to this epic |
| `--parent <key>` | — | — | items with this parent |
| `--open` | — | — | only unresolved work items |
| `--resolved` | — | — | only resolved work items |
| `--sort <field>` | — | `updated` | updated, created, key or status |
| `--order <direction>` | — | `desc` | asc or desc |
| `--created-since <when>` | — | — | created at or after (30d, 6w, 2024-01-31) |
| `--created-before <when>` | — | — | created before (30d, 6w, 2024-01-31) |
| `--updated-since <when>` | — | — | updated at or after (30d, 6w, 2024-01-31) |
| `--updated-before <when>` | — | — | updated before, i.e. untouched since then |
| `--stale <duration>` | — | — | only items not updated for this long (e.g. 90d) |
| `-o, --output <format>` | `default`, `json`, `markdown`, `plain` | `default` | output format |
| `--list` | — | — | print bare identifiers only, one per line (for shell scripts) |
| `-n, --limit <count>` | — | `50` | maximum number of rows (0 for no limit) |
| `--offset <count>` | — | — | skip this many rows |
| `--search <text>` | — | — | match text in the title / summary / body |

### `devcontext jira epics`

Alias: `epic`

List epics (work items of type epic)

```
devcontext jira epics [options] [key]
```

| Argument | Required | Description |
| -------- | -------- | ----------- |
| `key` | no | work item key (e.g. PROJ-123) to show in detail |

| Option | Values | Default | Description |
| --- | --- | --- | --- |
| `-p, --project <key>` | — | — | Jira project key, repeatable |
| `--site <name>` | — | — | Jira site name from the configuration |
| `--status <status>` | — | — | status name, repeatable |
| `--category <category>` | — | — | "To Do", "In Progress" or "Done" |
| `--assignee <name>` | — | — | assignee display name (substring match) |
| `--reporter <name>` | — | — | reporter display name (substring match) |
| `-l, --label <label>` | — | — | label filter, repeatable |
| `--component <name>` | — | — | component filter, repeatable |
| `--sprint <name>` | — | — | sprint name (substring match) |
| `--epic <key>` | — | — | items belonging to this epic |
| `--parent <key>` | — | — | items with this parent |
| `--open` | — | — | only unresolved work items |
| `--resolved` | — | — | only resolved work items |
| `--sort <field>` | — | `updated` | updated, created, key or status |
| `--order <direction>` | — | `desc` | asc or desc |
| `--created-since <when>` | — | — | created at or after (30d, 6w, 2024-01-31) |
| `--created-before <when>` | — | — | created before (30d, 6w, 2024-01-31) |
| `--updated-since <when>` | — | — | updated at or after (30d, 6w, 2024-01-31) |
| `--updated-before <when>` | — | — | updated before, i.e. untouched since then |
| `--stale <duration>` | — | — | only items not updated for this long (e.g. 90d) |
| `-o, --output <format>` | `default`, `json`, `markdown`, `plain` | `default` | output format |
| `--list` | — | — | print bare identifiers only, one per line (for shell scripts) |
| `-n, --limit <count>` | — | `50` | maximum number of rows (0 for no limit) |
| `--offset <count>` | — | — | skip this many rows |
| `--search <text>` | — | — | match text in the title / summary / body |

### `devcontext jira features`

Alias: `feature`

List features (work items of type feature)

```
devcontext jira features [options] [key]
```

| Argument | Required | Description |
| -------- | -------- | ----------- |
| `key` | no | work item key (e.g. PROJ-123) to show in detail |

| Option | Values | Default | Description |
| --- | --- | --- | --- |
| `-p, --project <key>` | — | — | Jira project key, repeatable |
| `--site <name>` | — | — | Jira site name from the configuration |
| `--status <status>` | — | — | status name, repeatable |
| `--category <category>` | — | — | "To Do", "In Progress" or "Done" |
| `--assignee <name>` | — | — | assignee display name (substring match) |
| `--reporter <name>` | — | — | reporter display name (substring match) |
| `-l, --label <label>` | — | — | label filter, repeatable |
| `--component <name>` | — | — | component filter, repeatable |
| `--sprint <name>` | — | — | sprint name (substring match) |
| `--epic <key>` | — | — | items belonging to this epic |
| `--parent <key>` | — | — | items with this parent |
| `--open` | — | — | only unresolved work items |
| `--resolved` | — | — | only resolved work items |
| `--sort <field>` | — | `updated` | updated, created, key or status |
| `--order <direction>` | — | `desc` | asc or desc |
| `--created-since <when>` | — | — | created at or after (30d, 6w, 2024-01-31) |
| `--created-before <when>` | — | — | created before (30d, 6w, 2024-01-31) |
| `--updated-since <when>` | — | — | updated at or after (30d, 6w, 2024-01-31) |
| `--updated-before <when>` | — | — | updated before, i.e. untouched since then |
| `--stale <duration>` | — | — | only items not updated for this long (e.g. 90d) |
| `-o, --output <format>` | `default`, `json`, `markdown`, `plain` | `default` | output format |
| `--list` | — | — | print bare identifiers only, one per line (for shell scripts) |
| `-n, --limit <count>` | — | `50` | maximum number of rows (0 for no limit) |
| `--offset <count>` | — | — | skip this many rows |
| `--search <text>` | — | — | match text in the title / summary / body |

### `devcontext jira outcomes`

Alias: `outcome`

List outcomes (work items of type outcome)

```
devcontext jira outcomes [options] [key]
```

| Argument | Required | Description |
| -------- | -------- | ----------- |
| `key` | no | work item key (e.g. PROJ-123) to show in detail |

| Option | Values | Default | Description |
| --- | --- | --- | --- |
| `-p, --project <key>` | — | — | Jira project key, repeatable |
| `--site <name>` | — | — | Jira site name from the configuration |
| `--status <status>` | — | — | status name, repeatable |
| `--category <category>` | — | — | "To Do", "In Progress" or "Done" |
| `--assignee <name>` | — | — | assignee display name (substring match) |
| `--reporter <name>` | — | — | reporter display name (substring match) |
| `-l, --label <label>` | — | — | label filter, repeatable |
| `--component <name>` | — | — | component filter, repeatable |
| `--sprint <name>` | — | — | sprint name (substring match) |
| `--epic <key>` | — | — | items belonging to this epic |
| `--parent <key>` | — | — | items with this parent |
| `--open` | — | — | only unresolved work items |
| `--resolved` | — | — | only resolved work items |
| `--sort <field>` | — | `updated` | updated, created, key or status |
| `--order <direction>` | — | `desc` | asc or desc |
| `--created-since <when>` | — | — | created at or after (30d, 6w, 2024-01-31) |
| `--created-before <when>` | — | — | created before (30d, 6w, 2024-01-31) |
| `--updated-since <when>` | — | — | updated at or after (30d, 6w, 2024-01-31) |
| `--updated-before <when>` | — | — | updated before, i.e. untouched since then |
| `--stale <duration>` | — | — | only items not updated for this long (e.g. 90d) |
| `-o, --output <format>` | `default`, `json`, `markdown`, `plain` | `default` | output format |
| `--list` | — | — | print bare identifiers only, one per line (for shell scripts) |
| `-n, --limit <count>` | — | `50` | maximum number of rows (0 for no limit) |
| `--offset <count>` | — | — | skip this many rows |
| `--search <text>` | — | — | match text in the title / summary / body |

### `devcontext jira bugs`

Alias: `bug`

List bugs (work items of type bug)

```
devcontext jira bugs [options] [key]
```

| Argument | Required | Description |
| -------- | -------- | ----------- |
| `key` | no | work item key (e.g. PROJ-123) to show in detail |

| Option | Values | Default | Description |
| --- | --- | --- | --- |
| `-p, --project <key>` | — | — | Jira project key, repeatable |
| `--site <name>` | — | — | Jira site name from the configuration |
| `--status <status>` | — | — | status name, repeatable |
| `--category <category>` | — | — | "To Do", "In Progress" or "Done" |
| `--assignee <name>` | — | — | assignee display name (substring match) |
| `--reporter <name>` | — | — | reporter display name (substring match) |
| `-l, --label <label>` | — | — | label filter, repeatable |
| `--component <name>` | — | — | component filter, repeatable |
| `--sprint <name>` | — | — | sprint name (substring match) |
| `--epic <key>` | — | — | items belonging to this epic |
| `--parent <key>` | — | — | items with this parent |
| `--open` | — | — | only unresolved work items |
| `--resolved` | — | — | only resolved work items |
| `--sort <field>` | — | `updated` | updated, created, key or status |
| `--order <direction>` | — | `desc` | asc or desc |
| `--created-since <when>` | — | — | created at or after (30d, 6w, 2024-01-31) |
| `--created-before <when>` | — | — | created before (30d, 6w, 2024-01-31) |
| `--updated-since <when>` | — | — | updated at or after (30d, 6w, 2024-01-31) |
| `--updated-before <when>` | — | — | updated before, i.e. untouched since then |
| `--stale <duration>` | — | — | only items not updated for this long (e.g. 90d) |
| `-o, --output <format>` | `default`, `json`, `markdown`, `plain` | `default` | output format |
| `--list` | — | — | print bare identifiers only, one per line (for shell scripts) |
| `-n, --limit <count>` | — | `50` | maximum number of rows (0 for no limit) |
| `--offset <count>` | — | — | skip this many rows |
| `--search <text>` | — | — | match text in the title / summary / body |

### `devcontext jira tasks`

Alias: `task`

List tasks (work items of type task)

```
devcontext jira tasks [options] [key]
```

| Argument | Required | Description |
| -------- | -------- | ----------- |
| `key` | no | work item key (e.g. PROJ-123) to show in detail |

| Option | Values | Default | Description |
| --- | --- | --- | --- |
| `-p, --project <key>` | — | — | Jira project key, repeatable |
| `--site <name>` | — | — | Jira site name from the configuration |
| `--status <status>` | — | — | status name, repeatable |
| `--category <category>` | — | — | "To Do", "In Progress" or "Done" |
| `--assignee <name>` | — | — | assignee display name (substring match) |
| `--reporter <name>` | — | — | reporter display name (substring match) |
| `-l, --label <label>` | — | — | label filter, repeatable |
| `--component <name>` | — | — | component filter, repeatable |
| `--sprint <name>` | — | — | sprint name (substring match) |
| `--epic <key>` | — | — | items belonging to this epic |
| `--parent <key>` | — | — | items with this parent |
| `--open` | — | — | only unresolved work items |
| `--resolved` | — | — | only resolved work items |
| `--sort <field>` | — | `updated` | updated, created, key or status |
| `--order <direction>` | — | `desc` | asc or desc |
| `--created-since <when>` | — | — | created at or after (30d, 6w, 2024-01-31) |
| `--created-before <when>` | — | — | created before (30d, 6w, 2024-01-31) |
| `--updated-since <when>` | — | — | updated at or after (30d, 6w, 2024-01-31) |
| `--updated-before <when>` | — | — | updated before, i.e. untouched since then |
| `--stale <duration>` | — | — | only items not updated for this long (e.g. 90d) |
| `-o, --output <format>` | `default`, `json`, `markdown`, `plain` | `default` | output format |
| `--list` | — | — | print bare identifiers only, one per line (for shell scripts) |
| `-n, --limit <count>` | — | `50` | maximum number of rows (0 for no limit) |
| `--offset <count>` | — | — | skip this many rows |
| `--search <text>` | — | — | match text in the title / summary / body |

### `devcontext jira tree`

Show the parents and children of a work item as a tree

```
devcontext jira tree [options] <key>
```

| Argument | Required | Description |
| -------- | -------- | ----------- |
| `key` | yes | work item key, e.g. PLAT-42 |

| Option | Values | Default | Description |
| --- | --- | --- | --- |
| `--depth <levels>` | — | `5` | how many levels below the item to show |
| `--no-ancestors` | — | — | do not walk up to the parents |
| `--links` | — | — | also show the GitHub pull requests and issues that reference each item |
| `-o, --output <format>` | `default`, `json`, `markdown`, `plain` | `default` | output format |
| `--list` | — | — | print bare identifiers only, one per line (for shell scripts) |

### `devcontext jira search`

Search work items by key, summary, description and comments

```
devcontext jira search [options] <query>
```

| Argument | Required | Description |
| -------- | -------- | ----------- |
| `query` | yes | text to look for |

| Option | Values | Default | Description |
| --- | --- | --- | --- |
| `-p, --project <key>` | — | — | Jira project key, repeatable |
| `-t, --type <type>` | — | — | work item type, repeatable |
| `--status <status>` | — | — | status name, repeatable |
| `--assignee <name>` | — | — | assignee display name (substring match) |
| `--open` | — | — | only unresolved work items |
| `--resolved` | — | — | only resolved work items |
| `--created-since <when>` | — | — | created at or after (30d, 6w, 2024-01-31) |
| `--created-before <when>` | — | — | created before (30d, 6w, 2024-01-31) |
| `--updated-since <when>` | — | — | updated at or after (30d, 6w, 2024-01-31) |
| `--updated-before <when>` | — | — | updated before, i.e. untouched since then |
| `--stale <duration>` | — | — | only items not updated for this long (e.g. 90d) |
| `-o, --output <format>` | `default`, `json`, `markdown`, `plain` | `default` | output format |
| `--list` | — | — | print bare identifiers only, one per line (for shell scripts) |
| `-n, --limit <count>` | — | `50` | maximum number of rows (0 for no limit) |
| `--offset <count>` | — | — | skip this many rows |
| `--search <text>` | — | — | match text in the title / summary / body |

### `devcontext jira sprints`

Alias: `sprint`

List sprints, or show one sprint with its work items

```
devcontext jira sprints [options] [id]
```

| Argument | Required | Description |
| -------- | -------- | ----------- |
| `id` | no | sprint id to show in detail |

| Option | Values | Default | Description |
| --- | --- | --- | --- |
| `--site <name>` | — | — | Jira site name, repeatable |
| `--board <id>` | — | — | board id, repeatable |
| `--state <state>` | — | — | future, active or closed, repeatable |
| `-o, --output <format>` | `default`, `json`, `markdown`, `plain` | `default` | output format |
| `--list` | — | — | print bare identifiers only, one per line (for shell scripts) |
| `-n, --limit <count>` | — | `50` | maximum number of rows (0 for no limit) |
| `--offset <count>` | — | — | skip this many rows |
| `--search <text>` | — | — | match text in the title / summary / body |

### `devcontext jira projects`

Alias: `project`

List the Jira projects that have been synced

```
devcontext jira projects [options]
```

| Option | Values | Default | Description |
| --- | --- | --- | --- |
| `-o, --output <format>` | `default`, `json`, `markdown`, `plain` | `default` | output format |
| `--list` | — | — | print bare identifiers only, one per line (for shell scripts) |
| `-n, --limit <count>` | — | `50` | maximum number of rows (0 for no limit) |
| `--offset <count>` | — | — | skip this many rows |
| `--search <text>` | — | — | match text in the title / summary / body |

### `devcontext jira fields`

Alias: `field`

List Jira fields and the friendly names configured for them

```
devcontext jira fields [options]
```

| Option | Values | Default | Description |
| --- | --- | --- | --- |
| `--mapped` | — | — | only fields with a configured name |
| `-o, --output <format>` | `default`, `json`, `markdown`, `plain` | `default` | output format |
| `--list` | — | — | print bare identifiers only, one per line (for shell scripts) |
| `-n, --limit <count>` | — | `50` | maximum number of rows (0 for no limit) |
| `--offset <count>` | — | — | skip this many rows |
| `--search <text>` | — | — | match text in the title / summary / body |

## `devcontext tickets`

Alias: `ticket`

GitHub issues and Jira work items as one list

```
devcontext tickets [options]
```

| Option | Values | Default | Description |
| --- | --- | --- | --- |
| `--source <source>` | — | — | github or jira, repeatable |
| `-c, --container <name>` | — | — | repository (acme/platform) or Jira project (PLAT), repeatable |
| `-t, --type <type>` | — | — | Bug, Story, Issue, ... repeatable |
| `-s, --state <state>` | — | `all` | open, closed or all |
| `--assignee <name>` | — | — | assigned to this person |
| `--person <id>` | — | — | raised by or assigned to this configured person, repeatable |
| `--team <id>` | — | — | raised by or assigned to a member of this team, repeatable |
| `--me` | — | — | shorthand for --person <the id me: names in devcontext.yaml> |
| `--types` | — | — | list the types present and how many carry each, instead |
| `--containers` | — | — | list the repositories and projects present, instead |
| `-o, --output <format>` | `default`, `json`, `markdown`, `plain` | `default` | output format |
| `--list` | — | — | print bare identifiers only, one per line (for shell scripts) |
| `-n, --limit <count>` | — | `50` | maximum number of rows (0 for no limit) |
| `--offset <count>` | — | — | skip this many rows |
| `--search <text>` | — | — | match text in the title / summary / body |

## `devcontext activity`

Aliases: `feed`, `changes`

Status changes, comments and reviews across GitHub and Jira, newest first

```
devcontext activity [options]
```

| Option | Values | Default | Description |
| --- | --- | --- | --- |
| `--since <when>` | — | `14d` | events at or after this point (7d, 6w, 2024-01-31) |
| `--until <when>` | — | — | events before this point |
| `--source <source>` | — | — | github or jira, repeatable |
| `-c, --container <name>` | — | — | repository (acme/platform) or Jira project (PLAT), repeatable |
| `-k, --kind <kind>` | — | — | status, comment or review, repeatable |
| `--by-person` | — | — | who was busy in the window, instead of what happened |
| `--person <id>` | — | — | only items involving this configured person, repeatable |
| `--team <id>` | — | — | only items involving a member of this team, repeatable |
| `--me` | — | — | shorthand for --person <the id me: names in devcontext.yaml> |
| `--no-bots` | — | — | hide items written by a bot |
| `--bots-only` | — | — | only items written by a bot |
| `-o, --output <format>` | `default`, `json`, `markdown`, `plain` | `default` | output format |
| `--list` | — | — | print bare identifiers only, one per line (for shell scripts) |
| `-n, --limit <count>` | — | `50` | maximum number of rows (0 for no limit) |
| `--offset <count>` | — | — | skip this many rows |
| `--search <text>` | — | — | match text in the title / summary / body |

## `devcontext people`

Alias: `person`

The configured people and bots, with their GitHub and Jira identities

```
devcontext people [options]
```

| Option | Values | Default | Description |
| --- | --- | --- | --- |
| `--team <id>` | — | — | only members of this team, repeatable |
| `--bots-only` | — | — | only the configured bots |
| `--no-bots` | — | — | only the humans |
| `--identities` | — | — | one row per identity, with what the data knows about it |
| `--unmapped` | — | — | names found in the data that belong to nobody, busiest first |
| `-o, --output <format>` | `default`, `json`, `markdown`, `plain` | `default` | output format |
| `--list` | — | — | print bare identifiers only, one per line (for shell scripts) |

## `devcontext teams`

Alias: `team`

The configured teams and their members

```
devcontext teams [options]
```

| Option | Values | Default | Description |
| --- | --- | --- | --- |
| `-o, --output <format>` | `default`, `json`, `markdown`, `plain` | `default` | output format |
| `--list` | — | — | print bare identifiers only, one per line (for shell scripts) |

## `devcontext serve`

Alias: `web`

Serve the React viewer and a JSON API for the local database

```
devcontext serve [options]
```

| Option | Description |
| --- | --- |
| `-p, --port <port>` | port to listen on |
| `--host <host>` | interface to bind to |

## `devcontext insights`

Aliases: `report`, `stats`

Cycle time, review latency, work in progress, stale items, flaky steps, sprints

```
devcontext insights [options] [section]
```

| Argument | Required | Description |
| -------- | -------- | ----------- |
| `section` | no | one of cycle-time, review-latency, wip, stale, flaky, sprint, burndown, velocity, flow, status-time; omit for an overview of all of them |

| Option | Values | Default | Description |
| --- | --- | --- | --- |
| `--since <when>` | — | `90d` | only consider activity at or after this (default 90d) |
| `--until <when>` | — | — | only consider activity before this |
| `-r, --repo <repo>` | — | — | GitHub repository, repeatable |
| `-p, --project <key>` | — | — | Jira project key, repeatable |
| `--stale-after <duration>` | — | `30d` | age at which open work counts as stale |
| `--min-runs <count>` | — | `5` | minimum runs before a step can be called flaky |
| `--sprint <id>` | — | — | sprint id for the sprint and burndown reports |
| `--board <id>` | — | — | board id, to keep velocity to one team |
| `--points` | — | — | burn story points rather than item counts |
| `-n, --limit <count>` | — | `15` | rows per section |
| `-o, --output <format>` | `default`, `json`, `markdown`, `plain` | `default` | output format |
| `--list` | — | — | print bare identifiers only, one per line (for shell scripts) |

## `devcontext digest`

Aliases: `standup`, `summary`

What happened in a window: shipped, started, reviewed, and still stuck

```
devcontext digest [options]
```

| Option | Values | Default | Description |
| --- | --- | --- | --- |
| `--since <when>` | — | `1w` | start of the window (1w, 3d, 2024-01-31) |
| `--until <when>` | — | — | end of the window; defaults to now |
| `-r, --repo <repo>` | — | — | GitHub repository, repeatable |
| `-p, --project <key>` | — | — | Jira project key, repeatable |
| `--person <name>` | — | — | a configured person id, or a raw login / display name, repeatable |
| `--me` | — | — | shorthand for --person <the id me: names in devcontext.yaml> |
| `--stale-after <duration>` | — | `30d` | age at which open work counts as stale |
| `--no-stale` | — | — | skip the stale section |
| `-n, --limit <count>` | — | `10` | rows per section |
| `-o, --output <format>` | `default`, `json`, `markdown`, `plain` | `default` | output format |
| `--list` | — | — | print bare identifiers only, one per line (for shell scripts) |

## `devcontext links`

Alias: `link`

Cross references between GitHub issues/pull requests and Jira work items

```
devcontext links [options] [ref]
```

| Argument | Required | Description |
| -------- | -------- | ----------- |
| `ref` | no | only links touching this reference (acme/platform#42 or PLAT-7) |

| Option | Values | Default | Description |
| --- | --- | --- | --- |
| `--rebuild` | — | — | recompute the links from the synced text before listing |
| `--via <source>` | — | — | branch, title, body, commit or comment, repeatable |
| `--high` | — | — | only high confidence links (branch names, titles, Jira fields) |
| `--from <source>` | — | — | github or jira |
| `--to <source>` | — | — | github or jira |
| `-o, --output <format>` | `default`, `json`, `markdown`, `plain` | `default` | output format |
| `--list` | — | — | print bare identifiers only, one per line (for shell scripts) |
| `-n, --limit <count>` | — | `50` | maximum number of rows (0 for no limit) |
| `--offset <count>` | — | — | skip this many rows |
| `--search <text>` | — | — | match text in the title / summary / body |

## `devcontext contributors`

Aliases: `who`, `contributor`

Who worked on an issue, pull request or work item, and what they did

```
devcontext contributors [options] [ref]
```

| Argument | Required | Description |
| -------- | -------- | ----------- |
| `ref` | no | a reference (acme/platform#42 or PLAT-7) |

| Option | Values | Default | Description |
| --- | --- | --- | --- |
| `--rollup` | — | — | include everything beneath it: child work items and linked pull requests |
| `--role <role>` | — | — | only this capacity (author, reviewer, commenter, ...) |
| `--by-item` | — | — | one row per contribution rather than one per person |
| `--rebuild` | — | — | recompute the table from the synced rows before listing |
| `-o, --output <format>` | `default`, `json`, `markdown`, `plain` | `default` | output format |
| `--list` | — | — | print bare identifiers only, one per line (for shell scripts) |
| `-n, --limit <count>` | — | `50` | maximum number of rows (0 for no limit) |
| `--offset <count>` | — | — | skip this many rows |
| `--search <text>` | — | — | match text in the title / summary / body |

## `devcontext history`

How many issues, pull requests or work items were open over time

```
devcontext history [options]
```

| Option | Values | Default | Description |
| --- | --- | --- | --- |
| `--from <when>` | — | `30d` | start of the window (30d, 2024-01-31) |
| `--to <when>` | — | `now` | end of the window |
| `--source <source>` | — | — | github or jira |
| `--container <name>` | — | — | one repository (acme/platform) or Jira project (PLAT) |
| `--kind <kind>` | — | — | issue, pull_request or workitem |
| `--assignee <person>` | — | — | only items assigned to this person at the time |
| `--sprint <id>` | — | — | only items in this sprint at the time |
| `--by-assignee` | — | — | a snapshot per person at the end of the window instead |
| `--rebuild` | — | — | recompute the history from the synced timelines before reading it |
| `-o, --output <format>` | `default`, `json`, `markdown`, `plain` | `default` | output format |
| `--list` | — | — | print bare identifiers only, one per line (for shell scripts) |
| `-n, --limit <count>` | — | `50` | maximum number of rows (0 for no limit) |
| `--offset <count>` | — | — | skip this many rows |
| `--search <text>` | — | — | match text in the title / summary / body |

## `devcontext audit`

What is stored locally, who is in it, and what a sync would fetch

```
devcontext audit [options] [section]
```

| Argument | Required | Description |
| -------- | -------- | ----------- |
| `section` | no | one of storage, content, people, secrets, config; omit for all of them |

| Option | Values | Default | Description |
| --- | --- | --- | --- |
| `--all` | — | — | include low confidence secret matches (keyword heuristics) |
| `-n, --limit <count>` | — | `25` | rows per section |
| `-o, --output <format>` | `default`, `json`, `markdown`, `plain` | `default` | output format |
| `--list` | — | — | print bare identifiers only, one per line (for shell scripts) |

## `devcontext mcp`

Serve the local database over the Model Context Protocol (stdio)

```
devcontext mcp [options]
```

| Option | Values | Default | Description |
| --- | --- | --- | --- |
| `--tools` | — | — | list the exposed tools and exit |
| `-o, --output <format>` | `default`, `json`, `markdown`, `plain` | `default` | output format |
| `--list` | — | — | print bare identifiers only, one per line (for shell scripts) |

## `devcontext agent`

Start the @devcontext/eve agent in dev mode (experimental)

```
devcontext agent [args...]
```

| Argument | Required | Description |
| -------- | -------- | ----------- |
| `args...` | no | arguments passed through to "eve dev", e.g. --no-ui |
